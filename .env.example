# DutySync Environment Variables
# Copy this file to .env.local and fill in your values
# .env.local is automatically ignored by git

# ==============================================================================
# REQUIRED: Authentication
# ==============================================================================

# NextAuth.js secret key (REQUIRED)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-here

# ==============================================================================
# REQUIRED: Supabase Configuration
# ==============================================================================

# Get these from your Supabase project: Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ==============================================================================
# OPTIONAL: GitHub Workflow Integration
# ==============================================================================

# For triggering GitHub Actions workflows from the client
# SECURITY WARNING: These are exposed in the client bundle!
# Use a fine-grained PAT with ONLY "Actions: Read and write" permission

NEXT_PUBLIC_GITHUB_OWNER=your-github-username
NEXT_PUBLIC_GITHUB_REPO=DutySync
NEXT_PUBLIC_GITHUB_TOKEN=github_pat_xxxx

# ==============================================================================
# OPTIONAL: Security Configuration
# ==============================================================================

# App Admin EDIPI (10-digit identifier for the primary admin)
NEXT_PUBLIC_APP_ADMIN=1234567890

# EDIPI Encryption Key (for encrypting EDIPIs in stored data)
# Use a strong random string: openssl rand -base64 32
NEXT_PUBLIC_EDIPI_KEY=your-encryption-key-here

# ==============================================================================
# SECURITY BEST PRACTICES
# ==============================================================================
#
# 1. AUTH_SECRET: Must be set - app will not work without it
#    Generate with: openssl rand -base64 32
#
# 2. GitHub Token: Use a fine-grained PAT with MINIMAL permissions
#    - Only grant "Actions: Read and write"
#    - Set token expiration to 90 days or less
#    - Regularly rotate the token
#
# 3. EDIPI Key: Use a strong, unique key
#    - Never commit this to version control
#    - Rotate if you suspect compromise
#
# 4. For production deployments:
#    - Consider using a backend proxy for GitHub API calls
#    - Implement proper secrets management (HashiCorp Vault, AWS Secrets Manager)
#    - Enable Supabase Row Level Security (RLS) policies
