# Supabase Configuration (Required for multi-user sync)
# Get these from your Supabase project: Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# GitHub Configuration (For JSON file backup/sync)
# WARNING: This is a server-side secret - do NOT use NEXT_PUBLIC_ prefix
GITHUB_WORKFLOW_TOKEN=your-github-token

# EDIPI Encryption Key (For encrypting sensitive IDs in JSON files)
# WARNING: This is a server-side secret - do NOT use NEXT_PUBLIC_ prefix
EDIPI_KEY=your-encryption-key

# Copy this file to .env.local and fill in your values
# .env.local is automatically ignored by git
